<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lgy.drive">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <uses-permission android:name="android.permission.CALL_PHONE"></uses-permission>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"></uses-permission>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activity.WecomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".ui.activity.SetActivity"></activity>
        <activity
            android:name=".ui.activity.LoginActivity"
            android:launchMode="singleTop"></activity>
        <activity android:name=".ui.activity.RegisterActivity"></activity>
        <activity android:name=".ui.activity.OrderListActivity"></activity>
        <activity android:name=".ui.activity.ForgetPasswordActivity"></activity>
        <activity android:name=".ui.activity.CompleteUserInforActivity"></activity>
        <activity android:name=".ui.activity.CompleteCarInforActivity"></activity>
        <activity android:name=".ui.activity.AddRecordActivity"></activity>
        <activity android:name=".ui.activity.OrderActivity"></activity>

        <meta-data
            android:name="com.lgy.drive.utils.imageutils.MyGlideModule"
            android:value="GlideModule" />
    </application>

</manifest>